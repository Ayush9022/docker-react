sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t mragent/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mragent/docker-react npm run test
